PORT=3000

# Credenciales de Microsoft Azure para OAuth2
# Estas credenciales se usan para que la aplicación se autentique directamente con Microsoft Graph
# La cuenta que uses para autenticarte debe tener acceso a la carpeta de OneDrive que quieres acceder
MICROSOFT_CLIENT_ID=tu-client-id-aqui
MICROSOFT_CLIENT_SECRET=tu-client-secret-aqui

# Tenant ID (Opcional)
# - "common": Permite cuentas personales (outlook.com, hotmail.com) y organizacionales (recomendado)
# - "consumers": Solo cuentas personales de Microsoft
# - "organizations": Solo cuentas organizacionales de Azure AD
# - Tu tenant específico: Solo cuentas de ese tenant
# Por defecto: "common"
MICROSOFT_TENANT_ID=common

# URI de redirección para OAuth2 (debe coincidir con la configurada en Azure)
# Por defecto: http://localhost:3000/auth/callback
REDIRECT_URI=http://localhost:3000/auth/callback

# ID de la carpeta raíz de OneDrive (Opcional)
# Si configuras este valor, la aplicación usará esta carpeta por defecto
ONEDRIVE_ROOT_FOLDER_ID=tu-folder-id-aqui
