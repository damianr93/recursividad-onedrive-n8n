PORT=3000

# ID de la carpeta raíz de OneDrive (Opcional)
# Si configuras este valor, la aplicación usará esta carpeta por defecto
ONEDRIVE_ROOT_FOLDER_ID=tu-folder-id-aqui

# NOTA: El accessToken se puede almacenar temporalmente usando el endpoint POST /set-token
# desde n8n. El token se almacena en memoria y se usa automáticamente si no se pasa en la petición.
